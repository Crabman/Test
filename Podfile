# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'testInsertions' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for testInsertions
  pod 'GRDB.swift/SQLCipher'
  pod 'SQLCipher', '~> 4.0'
  
  target 'testInsertionsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'testInsertionsUITests' do
    # Pods for testing
  end

end
